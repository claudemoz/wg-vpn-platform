# Server
APP_ENV=development
APP_PORT=8080

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=stoogo
DB_SSLMODE=disable
DB_TIMEZONE=UTC

# JWT
JWT_SECRET=change_me_in_production
JWT_EXPIRATION_HOURS=2160
